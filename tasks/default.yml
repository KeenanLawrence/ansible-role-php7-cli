---

- name: replace default locations with php7
  file:
    src: /opt/php72/bin/{{ item }}
    dest: /usr/bin/{{ item }}
    owner: root
    group: root
    state: link
  with_items:
    - phpize
    - php-config
    - php
    - phpdbg
    - pecl
    - phar
  become: yes
